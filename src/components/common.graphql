query ActiveUser {
    getActiveUser {
        ...UserFields
    }
}

mutation CreateActiveUser {
    createActiveUser {
        _id
    }
}

fragment UserFields on User {
    _id
    name
    email
    avatar
}