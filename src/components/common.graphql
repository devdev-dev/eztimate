query ActiveUser {
    getActiveUser {
        ...UserFields
    }
}

query GetUser($id: ID!) {
    getUser(id: $id) {
        ...UserFields
    }
}

fragment UserFields on User {
    _id
    name
    email
    avatar
}