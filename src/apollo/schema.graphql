type Mutation {
  userJoinTeam(teamId: String!): Team!

  teamCreate(teamName: String!): Team!

  issueCreate(name: String!): Issue
  issueUpdate(id: String!, name: String): Issue
  issueDelete(id: String!): Issue
  issueEstimate(id: String!): Team!

  estimateIssue(issueId: String!, value: String!): Issue
}

type Query {
  loggedInUser: User
  activeTeam: Team
}

type User {
  _id: String!
  email: String!
  teams: [Team]!
}

type Team {
  _id: String!
  name: String!
  users: [User]!
  issues: [Issue]!
  estimatedIssue: Issue
}

type Issue {
  _id: String!
  name: String!
  state: String!
  dateCreated: String!
  estimate: String
  estimates: [Estimate]!
}

type Estimate {
  _id: String!
  user: User!
  value: String!
}
