type Query {
    getActiveIssue: Issue
}

type Mutation {
    createActiveIssue: Issue
    estimateActiveIssue(value: String!): Issue
    resetActiveIssue: Issue

    createUser: User
}

type Issue {
    _id: ID!
    state: IssueState!
    estimates: [Estimate!]!
}

enum IssueState {
    COLLECT
    DISCUSS
}

type Estimate {
    _id: ID!
    value: String!
    user: User!
}

type User {
    _id: ID!
    name: String
}
